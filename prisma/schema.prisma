datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
    id       String @id
    warnings Int @default(0)
}

model Guild {
    id     String    @id
    prefix String @default("$")
    users  User[]
}
